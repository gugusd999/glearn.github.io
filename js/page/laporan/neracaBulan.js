define(['jquery', 'main', 'axios', 'crud', 'helper', 'select2', 'neracaBulan'], function($, main, axios, crud, helper, select2, neracaBulan) {
  let obj2 = {

    view: function() {
    	neracaBulan.view('bkk-menu');
    }
  }

  return obj2;

})